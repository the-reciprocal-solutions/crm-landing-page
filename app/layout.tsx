import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'Smart CRM Marketing Platform for Growing Businesses | The Reciprocal Solutions',
  description: 'Accelerate business growth with smart CRM marketing. From lead tracking to personalized email automation, our tools help you convert faster and scale smarter.',
  generator: 'https://thereciprocalsolutions.com/',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
